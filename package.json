{
  "name": "git-hooks-template",
  "version": "0.1.0",
  "private": true,
  "description": "开箱即用的 git hooks 预提交检查模板：husky v9 + lint-staged + prettier + commitlint + merge 跳过",
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "prepare": "husky",
    "typecheck": "echo \"(add your own typecheck script)\""
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.6",
    "prettier-plugin-compact-markdown-table": "^0.1.0"
  }
}
